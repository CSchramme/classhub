# Routes

The intended URL structure (see the project spec). Status is updated as each phase lands — an entry listed here is _planned_, not necessarily built yet.

| Route                                                                                                                                                                                       | Status                                 |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------- |
| `/`                                                                                                                                                                                         | ✅ Implemented (Phase 1, landing page) |
| `/login`, `/register`, `/setup/[token]`                                                                                                                                                     | Planned — Phase 3/4                    |
| `/home`, `/home/hausaufgaben`, `/home/todos`, `/home/stundenplan`, `/home/termine`, `/home/pruefungen`, `/home/faecher`                                                                     | Planned — Phase 7–11                   |
| `/cloud`, `/cloud/privat`, `/cloud/klasse`, `/cloud/klasse/[classSlug]`                                                                                                                     | Planned — Phase 12–13                  |
| `/klasse`, `/klasse/[classSlug]/...`                                                                                                                                                        | Planned — Phase 5–6                    |
| `/ki`, `/ki/[conversationId]`                                                                                                                                                               | Planned — Phase 14                     |
| `/profil`                                                                                                                                                                                   | Planned                                |
| `/einstellungen`, `/einstellungen/konto`, `/einstellungen/sicherheit`, `/einstellungen/benachrichtigungen`                                                                                  | Planned                                |
| `/admin`, `/admin/benutzer`, `/admin/benutzer/[userId]`, `/admin/schulen`, `/admin/schuljahre`, `/admin/klassen`, `/admin/klassen/[classId]`, `/admin/speicher`, `/admin/ki`, `/admin/logs` | Planned — Phase 4–5                    |
